(function(root) {
    var g = root.gamification = {};

    // var defaults = {};

    // g.setGamingDefaults = function(data) {
    //     if (!data.profile.gamification) {

    //     }
    // };

    // g.score = function() {};


    // Meteor.users.find().observe({
    //     added: g.setGamingDefaults,
    //     changed: g.setGamingDefaults
    // });

})(this);