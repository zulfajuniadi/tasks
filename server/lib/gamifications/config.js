;
(function(root) {
    // var g = root.gamification;

    // g.leaderboard();

    // g.score({
    //     type: 'xp',
    //     name: 'XP',
    //     title: 'Experience Points',
    //     levels: [],
    //     actions: {
    //         'Tasks': {
    //             callbacks: {
    //                 watch: ['_id', 'state'],
    //                 create: 5,
    //                 update: function(newData, oldData) {
    //                     if (newData.state !== oldData.state)
    //                         return 3;
    //                     return 0;
    //                 }
    //             }
    //         }
    //     }
    // });

    // g.score({
    //     type: 'rp',
    //     name: 'RP',
    //     title: 'Redeemable Points',
    //     redeemables: [],
    //     actions: {
    //         'Tasks': {
    //             callbacks: {
    //                 watch: ['_id', 'state'],
    //                 create: 5,
    //                 update: function(newData, oldData) {
    //                     if (newData.state !== oldData.state)
    //                         return 3;
    //                     return 0;
    //                 }
    //             }
    //         }
    //     }
    // });

    // g.leaderboard({

    // })

})(this);